# Aliasing : 동일한 데이터 요소나 포인트를 지칭하는 수단으로 사용되는 대체 레이블, 별명


x = 5 # 정수 5가 변수 x에 지정됨
y = X # x의 값이 y에 지정 => 변수 y는 변수 x의 가명(Alias)
y = 3 # 정수 3이 변수 y에 지정
print(x)
print(y)


x = [2,3,5,7,11]
y = x
y[2] = 4
print(x)
print(y)

x = [2,3,5,7,11]
y = list(x) # y는 list(x)라는 리스트를 새로 만듦. 즉 x와 별개의 리스트! 따라서 리스트 x의 값은 그대로 유지됨.
y[2] = 4
print(x)
print(y)




