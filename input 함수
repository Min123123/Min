# input 

input('곱셈할 두 수를 입력하시오. 예) 10 5').split() # split 함수를 이용해서 두 숫자를 분리시킴.

# 입력되는 값이 2개이기 때문에 2개의 변수를 사용함.

a,b = input('곱셈할 두 수를 입력하시오. 예) 10 5').split() # 이 때, input함수를 통해 대입되는 값은 문자형이다.

# 사칙연산을 위해 입력받는 두 문자열을 정수형으로 바꿔줘야 함! => int함수 사용

print(int(a) * int(b))

